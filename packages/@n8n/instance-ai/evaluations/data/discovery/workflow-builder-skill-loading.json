{
	"id": "workflow-builder-skill-loading",
	"userMessage": "Use the workflow-builder skill to create a simple workflow named Lead intake that starts with a manual trigger and sets a status field to new.",
	"expectedToolInvocations": {
		"allOfToolCalls": [
			{
				"toolName": "load_skill",
				"argsContainAny": ["workflow-builder"]
			},
			{
				"toolName": "workflows",
				"argsContainAny": ["create", "Lead intake"]
			}
		],
		"noneOf": [
			"build-workflow",
			"build-workflow-with-agent",
			"delegate",
			"spawn_sub_agent:workflow-builder",
			"spawn_sub_agent:delegate"
		]
	},
	"rationale": "Regression coverage for workflow-builder runtime skill routing. Workflow building must load workflow-builder and call workflows(action=\"create\"|\"update\") directly; the forbidden entries assert legacy workflow-building delegation paths are not used."
}
